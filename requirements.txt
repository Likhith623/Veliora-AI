# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Veliora.AI — Python Dependencies
# No PyTorch, no sentence-transformers, no heavy ML.
# All AI processing via external APIs.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ─── Core Framework ───
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ─── Data Validation ───
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── HTTP Client (for all external API calls) ───
httpx==0.28.1

# ─── Supabase ───
supabase==2.12.0

# ─── Environment ───
python-dotenv==1.0.1

# ─── File Upload Handling ───
python-multipart==0.0.20

# ─── WebSocket Support (included in uvicorn[standard]) ───
websockets==14.1

# ─── Deepgram SDK (Speech-to-Text) ───
deepgram-sdk==3.9.0

# ─── Cartesia SDK (Text-to-Speech) ───
cartesia==1.3.0

# ─── JWT Token Validation ───
PyJWT==2.10.1

# ─── Async Scheduling ───
apscheduler==3.10.4
