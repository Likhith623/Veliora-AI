/**
 * __awaiter - TypeScript's compiled async/await helper function
 * 
 * This is automatically generated by the TypeScript compiler to provide async/await
 * functionality in environments that may not have native support or for compatibility.
 * It converts async functions into promise-based generators for broader browser support.
 */


var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    /**
     * Helper function to ensure values are properly wrapped as promises
     * If the value is already a promise of the correct type, return it
     * Otherwise, create a new promise that immediately resolves with the value
     */
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
        // Return a new promise that manages the generator execution

    return new (P || (P = Promise))(function (resolve, reject) {
                // Handle successful generator steps

        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
                // Handle generator errors/exceptions

        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
                // Process each step of the generator, resolving or continuing as needed

        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
                // Start the generator execution

        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
function watchProgress(response, progressCallback) {
    return __awaiter(this, void 0, void 0, function* () {
        if (!response.body || !response.headers)
            return;
        const reader = response.body.getReader();
        const contentLength = Number(response.headers.get('Content-Length')) || 0;
        let receivedLength = 0;
        // Process the data
        const processChunk = (value) => __awaiter(this, void 0, void 0, function* () {
            // Add to the received length
            receivedLength += (value === null || value === void 0 ? void 0 : value.length) || 0;
            const percentage = Math.round((receivedLength / contentLength) * 100);
            progressCallback(percentage);
        });
        const read = () => __awaiter(this, void 0, void 0, function* () {
            let data;
            try {
                data = yield reader.read();
            }
            catch (_a) {
                // Ignore errors because we can only handle the main response
                return;
            }
            // Continue reading data until done
            if (!data.done) {
                processChunk(data.value);
                yield read();
            }
        });
        read();
    });
}
function fetchBlob(url, progressCallback, requestInit) {
    return __awaiter(this, void 0, void 0, function* () {
        // Fetch the resource
        const response = yield fetch(url, requestInit);
        if (response.status >= 400) {
            throw new Error(`Failed to fetch ${url}: ${response.status} (${response.statusText})`);
        }
        // Read the data to track progress
        watchProgress(response.clone(), progressCallback);
        return response.blob();
    });
}
const Fetcher = {
    fetchBlob,
};
export default Fetcher;
