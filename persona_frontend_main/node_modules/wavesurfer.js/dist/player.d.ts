/**
 * Import EventEmitter class and GeneralEventTypes interface from the event-emitter module
 * 
 * - EventEmitter: Base class that provides event handling capabilities (publish-subscribe pattern)
 * - GeneralEventTypes: Type definition for defining custom event structures with type safety
 * 
 * The Player class extends EventEmitter to enable event-driven communication for audio playback events
 * such as play, pause, timeupdate, ended, etc. The GeneralEventTypes allows for strongly-typed
 * event handling when using the Player with TypeScript.
 */

import EventEmitter, { type GeneralEventTypes } from './event-emitter.js';
type PlayerOptions = {
    /**
 * PlayerOptions - Configuration object for initializing a Player instance
 * 
 * Defines optional settings that control the behavior and setup of the audio player:
 * - media: Existing HTML media element to use instead of creating a new one
 * - mediaControls: Whether to show native browser media controls
 * - autoplay: Whether to automatically start playing when media is loaded
 * - playbackRate: Initial playback speed multiplier (1.0 = normal speed)
 */
    media?: HTMLMediaElement;
    mediaControls?: boolean;
    autoplay?: boolean;
    playbackRate?: number;
};
declare class Player<T extends GeneralEventTypes> extends EventEmitter<T> {
    protected media: HTMLMediaElement;
    private isExternalMedia;
    constructor(options: PlayerOptions);
    protected onMediaEvent<K extends keyof HTMLElementEventMap>(event: K, callback: (ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): () => void;
    protected getSrc(): string;
    private revokeSrc;
    private canPlayType;
    protected setSrc(url: string, blob?: Blob): void;
    protected destroy(): void;
    protected setMediaElement(element: HTMLMediaElement): void;
    /** Start playing the audio */
    play(): Promise<void>;
    /** Pause the audio */
    pause(): void;
    /** Check if the audio is playing */
    isPlaying(): boolean;
    /** Jump to a specific time in the audio (in seconds) */
    setTime(time: number): void;
    /** Get the duration of the audio in seconds */
    getDuration(): number;
    /** Get the current audio position in seconds */
    getCurrentTime(): number;
    /** Get the audio volume */
    getVolume(): number;
    /** Set the audio volume */
    setVolume(volume: number): void;
    /** Get the audio muted state */
    getMuted(): boolean;
    /** Mute or unmute the audio */
    setMuted(muted: boolean): void;
    /** Get the playback speed */
    getPlaybackRate(): number;
    /** Check if the audio is seeking */
    isSeeking(): boolean;
    /** Set the playback speed, pass an optional false to NOT preserve the pitch */
    setPlaybackRate(rate: number, preservePitch?: boolean): void;
    /** Get the HTML media element */
    getMediaElement(): HTMLMediaElement;
    /** Set a sink id to change the audio output device */
    setSinkId(sinkId: string): Promise<void>;
}
export default Player;
/**
 * Export Player as the default export for this module
 * 
 * The Player class provides a complete audio playback solution with:
 * - Event-driven architecture extending EventEmitter
 * - Full HTML5 media element control and management
 * - Type-safe event handling with TypeScript generics
 * - Audio output device selection support
 * - Playback rate control with optional pitch preservation
 * - Standard media controls (play, pause, seek, volume, mute)
 * 
 * This export allows importing the Player class as:
 * import Player from './player.js'
 */
