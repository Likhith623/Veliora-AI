/** Decode an array buffer into an audio buffer */
declare function decode(audioData: ArrayBuffer, sampleRate: number): Promise<AudioBuffer>;
/** Create an audio buffer from pre-decoded audio data */
declare function createBuffer(channelData: Array<Float32Array | number[]>, duration: number): AudioBuffer;
/** Create an audio buffer from pre-decoded audio data */
declare const Decoder: {
/**
 * Decoder - Audio decoding utilities for WaveSurfer
 * 
 * Provides methods for:
 * - Decoding raw audio data (ArrayBuffer) into AudioBuffer format
 * - Creating AudioBuffer instances from pre-decoded channel data
 * - Supporting various audio formats and sample rates
 */
    decode: typeof decode;   /** Decode raw audio data into an AudioBuffer */
    createBuffer: typeof createBuffer;  /** Create an AudioBuffer from channel data arrays */
};
export default Decoder;
